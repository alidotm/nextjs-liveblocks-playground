import { FC } from "react";

const Header: FC = () => {
	return (
		<header className="w-full bg-white fixed top-0 flex justify-center items-center font-bold text-3xl py-6">
			Nextjs x Liveblocks
		</header>
	);
};

export default Header;
